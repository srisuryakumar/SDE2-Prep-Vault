# 03 — Career and Networking

## 1. LinkedIn Strategy
- **Headline:** `Backend Engineer | Java · Spring Boot · Kubernetes · Distributed Systems | Building Scalable Systems | Open to SDE-2 Roles`
- **Cadence:** Post 2x per week (Tuesday 8 AM IST, Friday 6 PM IST).
- **Engagement:** 20 min/day commenting on posts from target company engineers.

## 2. Technical Blog Strategy
- **Platform:** Hashnode (custom domain) or Dev.to. Cross-post from Hashnode to Dev.to with canonical URLs.
- **Cadence:** 1 post every 2 weeks starting Week 4. Minimum 10 posts over the 120 days.
- **Workflow:** Every published blog post must be shared on LinkedIn on the same day.

## 3. Architecture Write-Ups & ADRs
For `order-management-api` and `scalable-ecommerce-platform`, you must document your engineering decisions:
- **ADRs (Architecture Decision Records):** Write a brief (200-word) document for every major technical choice (e.g., "Why I chose Kafka over HTTP calls").
- **Diagrams:** Include an `architecture.png` file in the `docs/` folder of the repo.
- **Load Testing:** Include `load-test-results.md` showing performance metrics (using k6/JMeter).

## 4. Networking CRM & Outreach
Maintain a tracker with: Name, Company, Role, LinkedIn URL, Connection Date, Message Sent, Response, Follow-up.
**Message Templates:**
- *Recruiter:* "Hi [Name], I'm a Backend Engineer with 3 YOE transitioning to distributed systems. I've built [Project] handling [Scale]. I'm actively targeting SDE-2 roles at [Company] — would you be open to a quick chat?"
- *Alumni:* "Hi [Name], fellow [College] alum here! I'm 3 years into my career and targeting SDE-2 at [Company]. Would you be willing to share your experience?"

## 5. Application Timeline
- **Day 30:** Practice Targets (Tier C)
- **Day 45:** Strong Targets (Tier B)
- **Day 60+:** Primary Targets (Tier A)

## Full Networking Message Templates (All 5)

### Template 1 — Technical Opener (For Backend Engineers)
Hi [Name], your [post/article] on [specific topic] was the most useful thing
I read this week — your point about [specific insight] changed how I'm
approaching [related problem in my project].

I'm a backend engineer going deep on Java/Kafka/Kubernetes and building a
scalable microservices platform. Would love to connect and follow your work.

---

### Template 2 — Technical Recruiters (Direct and Value-Forward)
Hi [Name], I'm a Backend Engineer with 3 YOE (Java, Spring Boot,
Kubernetes, Kafka) actively targeting SDE-2 roles at [Company].

Built: [one-line project description] — [GitHub link]

Are there backend engineering roles I should apply for? Happy to share
my resume.

---

### Template 3 — Engineering Managers (Vision-Level)
Hi [Name], I've been following [Company]'s engineering blog — the post
about [specific post] was particularly interesting. Your team's approach
to [specific challenge] is something I'm studying in my own distributed
systems work.

I'm an engineer transitioning into backend systems engineering and would
value 15 minutes to learn how your team approaches [X].

---

### Template 4 — Startup Founders (Problem-Solution)
Hi [Name], [Company]'s work on [specific problem] caught my attention.
I'm a Backend Engineer building expertise in [relevant area] — I've
shipped [specific thing] that tackles a similar challenge.

I'm exploring opportunities and believe I could contribute to [specific
thing their company needs]. Would you be open to a conversation?

---

### Template 5 — College Alumni (Warm, Specific Ask)
Hi [Name], fellow [College] alum here (batch [year])!

I see you're doing great work at [Company] — congratulations. I'm 3
years into my software engineering career and targeting SDE-2 backend
roles. [Company] is high on my list.

Would you be willing to share your experience there, or possibly refer
me if you think I'd be a good fit? Happy to connect on a call if easier.

## Offer Negotiation Scripts

### First Response to Any Offer (Buy Time)

When offer arrives by phone or email, say exactly this:

> "Thank you so much — I'm genuinely excited about this opportunity and
> the team. I want to give this the thoughtful consideration it deserves.
> Would it be alright if I get back to you by [3 business days from today]?"

Never accept on the call. Never reject on the call. Always buy time.

Before responding: check Levels.fyi, Glassdoor, and Blind for this exact
role and level at this exact company.

---

### Scenario 1 — Single Offer, No Competing Offers

Call back script:

> "Hi [Recruiter Name], I've had a chance to thoroughly review the offer
> and I'm really excited about joining [Company] and the work your team
> is doing on [specific engineering challenge].
>
> Based on my research on Levels.fyi and recent market data for backend
> SDE-2 roles with Kubernetes and distributed systems experience, I was
> expecting the base to be closer to ₹[your target + 10%]. Is there any
> flexibility to move in that direction?"
>
> [Silence. Let them respond. Do not fill the silence.]
>
> If they say no flexibility: "I understand. Could we discuss the joining
> bonus or an earlier performance review timeline instead?"
>
> If they counter: "That's helpful. Let me confirm in writing — could you
> send me the updated offer letter and I'll sign within 24 hours."

---

### Scenario 2 — Competing Offer From Another Company

> "Hi [Recruiter], I have to be transparent with you — I have another
> offer on the table from [Company B] at ₹[X] base. [Company A] is
> genuinely my first choice because of [specific reason: team, tech stack,
> scale of problems]. If you're able to come closer to ₹[X], I'm ready
> to sign today and withdraw from the other process."
>
> [Do not bluff. Only use this if the competing offer is real.]

---

### Scenario 3 — Offer Below Minimum, Dream Company

> "Hi [Recruiter], I really appreciate the offer and I'm very excited
> about [Company]. I have to be honest — the current base of ₹[X] is
> below what I need to move forward given [current responsibilities /
> competing offers / market rate for my skill set in Kafka and Kubernetes].
>
> I'm specifically targeting ₹[minimum] as a base. Is that a conversation
> we can have with the hiring manager? I want to find a way to make this
> work because [Company] is exactly where I want to be."

---

### What to Negotiate If Base Is Fixed

If they say the base cannot change, negotiate in this order:
1. Joining bonus (one-time, zero recurring cost to company)
2. First performance review at 6 months instead of 12
3. Remote flexibility (saves commute time and cost)
4. Additional PTO days
5. ESOP cliff reduction (4-year vest, 1-year cliff → ask for 6-month cliff)

### ESOP Valuation Rule

For ESOPs at pre-IPO companies: value them at 10% of the paper value
unless the company has a clear IPO or secondary market in the next
2 years. A ₹1 Cr ESOP grant at a Series B startup = ₹10 LPA in
realistic value for negotiation purposes.

### Final Rule

Never accept verbally. Always get the written offer letter before
giving notice at your current company. Written offer → sign → then
and only then, resign.

## LinkedIn 34-Post Calendar (All Posts Written in Full)

One post every Tuesday and Friday for 17 weeks.
Post at 8:00 AM IST on Tuesdays, 6:00 PM IST on Fridays.

---

**POST 1 — Week 1, Tuesday**
Topic: Journey Announcement

Day 1 of 120.

I'm a frontend engineer with 3 years of experience. I'm spending the
next 120 days rebuilding myself as a backend engineer specialising in
distributed systems.

Why? Because the systems I want to build — ones that handle millions of
users, require careful thinking about consistency and scale — demand
backend depth I don't currently have.

What I'll share every week:
→ What I'm learning (Java, Spring Boot, Kubernetes, Kafka)
→ What I'm building (6 projects, all public on GitHub)
→ What breaks (because things always break)

Current: ₹5 LPA, frontend engineer.
Target: ₹50 LPA, SDE-2 backend role. 120 days.

Starting now.

GitHub: [link]

#LearningInPublic #BackendEngineering #120DayChallenge

---

**POST 2 — Week 1, Friday**
Topic: Java Integer cache trap

Java Integer trap that would have caused a production bug.

Integer a = 127; Integer b = 127;
a == b → true ✓

Integer c = 128; Integer d = 128;
c == d → false ✗

Same logic. Different result. Why?

Java caches Integer objects between -128 and 127.
Within that range: a and b point to the SAME cached object.
Outside it: two different objects are created.

== compares object references, not values.
So c == d returns false — two different objects holding the same value.

The fix: always use .equals() for Integer comparison.
Or better: use int (primitive), where == always compares values.

Discovered this on Day 2. Going deeper into JVM memory model tomorrow.

#Java #BackendEngineering #LearningInPublic

---

**POST 3 — Week 2, Tuesday**
Topic: JVM Memory Model Diagram

I spent 2 hours understanding JVM memory. Here's what I drew.

[ATTACH: Hand-drawn or Canva diagram of JVM memory areas]

The diagram shows:
→ Heap: shared by all threads (Eden → Survivor → Old Generation)
→ Stack: one per thread, holds method call frames
→ Metaspace: class metadata (replaced PermGen in Java 8)

Why it matters in production:
- OutOfMemoryError: Java heap space → Eden or Old Gen is full
- StackOverflowError → recursion too deep
- OutOfMemoryError: Metaspace → too many dynamically loaded classes

Day 9 insight: when an object 'escapes' a method, the JIT cannot
stack-allocate it. That's why short-lived objects inside tight loops
are not always free — they put pressure on Eden and trigger Minor GCs.

Drawing systems you're learning beats watching someone else explain them.

#Java #JVM #BackendEngineering

---

**POST 4 — Week 2, Friday**
Topic: Race Condition Proof

I reproduced a race condition today. Here's the proof.

Expected output: Count = 10000
Actual output: Count = 9843 (different every run)

Why?

count++ is not atomic. It's three operations:
1. Read count from memory
2. Increment in register
3. Write back to memory

Thread 1 reads 500. Thread 2 reads 500.
Both increment to 501. Both write 501.
One increment is permanently lost.

Three fixes:
1. synchronized → mutex, one thread at a time, correct but slower
2. AtomicInteger → CAS hardware instruction, no lock, fast
3. volatile → fixes visibility only, NOT atomicity, wrong tool here

Lesson: debugging concurrency bugs is 10× harder than causing them.

Code reproducing this: [GitHub link]

#Java #Concurrency #BackendEngineering

---

**POST 5 — Week 3, Tuesday**
Topic: To-Do API Launch

My first Spring Boot API is live.

GitHub: [link] | Live Swagger UI: [link]

What it does:
- CRUD task management with JWT authentication
- PostgreSQL backend with Flyway migrations
- 80%+ test coverage with JUnit 5 and TestContainers

3 things I learned building it:

1. The N+1 problem — fetching 10 tasks ran 11 queries. Fixed with JOIN FETCH.
2. Flyway migrations — versioned SQL scripts mean no more "works on my machine"
3. TestContainers > H2 — tests now run against real PostgreSQL, catching real bugs

The live Swagger UI link means anyone can try the API without reading code.
That feels like the right way to build a portfolio.

Next: Order Management API with Kafka, Redis, and circuit breakers.

#SpringBoot #Java #BackendEngineering

---

**POST 6 — Week 3, Friday**
Topic: DSA Pattern Visual

The Two Pointers pattern has 4 variations. Here's when to use each.

[ATTACH: Diagram showing 4 variations]

1. Opposite ends (sorted array) → pair sum, container with most water
2. Fast-slow (same direction) → remove duplicates, detect cycle
3. Fixed sliding window → max sum of size k
4. Variable sliding window → longest substring without repeating chars

The visual that changed how I recognise patterns:

If the problem asks for a PAIR in a sorted array → opposite ends
If it asks for a RANGE satisfying a condition → sliding window
If it involves a linked list → fast-slow

120 problems in. These 4 templates cover roughly 30% of array/string mediums.

#DSA #LeetCode #BackendEngineering

---

**POST 7 — Week 4, Tuesday**
Topic: HashMap Internals

HashMap is not just a key-value store. Here's what happens under the hood.

[ATTACH: ASCII or Canva diagram of HashMap bucket array]

Step 1: key.hashCode() is called
Step 2: The hash is XOR-shifted: hash ^ (hash >>> 16)
        (This spreads high bits into low bits to reduce clustering)
Step 3: Bucket index = hash & (capacity - 1)
        (Bitwise AND works because capacity is always a power of 2)
Step 4: Collision? → Linked list in bucket
        > 8 entries in one bucket? → Converts to a Red-Black tree

Resize rule: when size > capacity × 0.75, double and rehash everything.

Why this matters for interviews:
"Why do you always override hashCode() with equals()?"
→ Because equal objects MUST have equal hashCodes. Break this contract
and your object becomes impossible to find in the map.

#Java #DataStructures #BackendEngineering

---

**POST 8 — Week 4, Friday**
Topic: LeetCode Patterns Carousel

[CAROUSEL POST — 5 slides]
Slide 1: Title — "5 DSA Patterns Covering 70% of Interview Problems"
Slide 2: Sliding Window — when to use, one-line template
Slide 3: Two Pointers — when to use, one-line template
Slide 4: Prefix Sum — when to use, one-line template
Slide 5: DFS on Trees — when to use, recursive template

Caption:
30 days of LeetCode and these 5 patterns have covered 70% of the
problems I've encountered. The skill is not memorising solutions.
It's learning to recognise which pattern fits in the first 2 minutes.

[ATTACH: Carousel]

#DSA #LeetCode #BackendEngineering

---

**POST 9 — Week 5, Tuesday**
Topic: Order Management API Architecture

Just pushed the Order Management API to GitHub.

GitHub: [link] | Swagger: [link]

Architecture in one diagram: [ATTACH]

What's inside:
→ JWT authentication via Spring Security filter chain
→ Redis cache (Cache-Aside pattern, 15-min TTL)
→ Kafka events (OrderCreated, OrderUpdated) with Dead Letter Queue
→ Resilience4j circuit breaker on every external service call
→ TestContainers: tests run against real PostgreSQL, Redis, and Kafka
→ Flyway migrations versioning every schema change
→ GitHub Actions CI/CD pipeline: test → Docker build → GHCR push

The biggest learning: testing with TestContainers caught 3 bugs
that H2 never would have. Real PostgreSQL, real constraints.

#SpringBoot #Java #BackendEngineering #DistributedSystems

---

**POST 10 — Week 5, Friday**
Topic: @Transactional deep dive

How @Transactional actually works — and the trap that breaks it silently.

Most developers think:
@Transactional = database transaction. Done.

What actually happens:
Spring creates a PROXY around your @Service class.
The proxy intercepts method calls, opens a transaction,
calls your method, then commits or rolls back.

The hidden trap — self-invocation:

@Service
public class OrderService {
    @Transactional
    public void processOrder(Long id) {
        updateStatus(id);
        sendNotification(id); // ← @Transactional IGNORED
    }

    @Transactional(propagation = REQUIRES_NEW)
    public void sendNotification(Long id) {
        // This does NOT run in a new transaction!
        // Called directly on 'this', not through the proxy.
    }
}

Fix: move sendNotification() to a separate @Service.

This is the most common Spring interview question and the most
common production bug I've seen in Spring Boot codebases.

#SpringBoot #Java #BackendEngineering

---

**POST 11 — Week 6, Tuesday**
Topic: Kafka Partitions and Consumer Groups

Kafka confused me for two weeks. Then I drew this and it clicked.

[ATTACH: Diagram of topic with 3 partitions and 2 consumer groups]

Key rules:
→ A partition is read by exactly ONE consumer per group at a time
→ More partitions = more parallelism within a group
→ Consumers > partitions = idle consumers (wasted)
→ Two consumer groups = independent reads of the same topic

Why partition by key?
If you process Order events, partition by order_id.
This guarantees all events for the same order arrive in sequence
at the same consumer. Order matters for stateful processing.

Why consumer lag is your most important Kafka metric:
Lag = latest offset − current offset per partition
Rising lag = your consumers can't keep up with producers
Alert when lag exceeds your SLA threshold.

#Kafka #DistributedSystems #BackendEngineering

---

**POST 12 — Week 6, Friday**
Topic: TestContainers vs H2

I switched from H2 to TestContainers mid-project. Here's what changed.

With H2 (in-memory DB):
- Tests ran in 8 seconds
- Missed: PostgreSQL-specific syntax (array columns, JSONB queries)
- Missed: Flyway migration failures on real PostgreSQL types
- Missed: HikariCP connection pool exhaustion under load

With TestContainers (real PostgreSQL in Docker):
- Tests run in 23 seconds
- Caught: 3 bugs that H2 silently ignored
- Caught: a race condition in the seat booking endpoint
- Now: I can confidently say "tests pass against production database"

The 15-second trade-off is worth it every time.

Setup is surprisingly simple:
@Testcontainers
class OrderServiceTest {
    @Container
    static PostgreSQLContainer<?> postgres =
        new PostgreSQLContainer<>("postgres:16-alpine");

    @DynamicPropertySource
    static void configure(DynamicPropertyRegistry r) {
        r.add("spring.datasource.url", postgres::getJdbcUrl);
    }
}

#SpringBoot #Testing #BackendEngineering

---

**POST 13 — Week 7, Tuesday**
Topic: Rate Limiter Algorithm Comparison

4 rate limiting algorithms. Each has a different trade-off.

[ATTACH: Comparison diagram]

Fixed Window:
- Simple: increment counter, reset every minute
- Problem: burst at window boundary (180 req in 2 seconds across reset)

Sliding Window Log:
- Accurate: store timestamp of every request, count within window
- Problem: memory grows linearly with request count

Sliding Window Counter:
- Hybrid: blend current + previous window counts
- Formula: count = current_window + previous_window × (1 - elapsed%)
- Best accuracy/memory balance. What I use in production.

Token Bucket:
- Tokens added at constant rate, consumed per request
- Allows controlled bursts up to bucket size
- Used by AWS API Gateway and most cloud providers

For distributed rate limiting:
All of these need Redis to share state across instances.
The sliding window counter maps cleanly to Redis INCR + EXPIRE.

#DistributedSystems #BackendEngineering #SystemDesign

---

**POST 14 — Week 7, Friday**
Topic: Resilience4j Circuit Breaker

How I protect the Order API from payment gateway failures.

[ATTACH: Circuit Breaker state diagram: CLOSED → OPEN → HALF_OPEN]

Without a circuit breaker:
Payment gateway goes down for 5 seconds.
100 threads pile up waiting for responses.
Connection pool exhausted. Order service goes down too.
One failure cascades into total outage.

With Resilience4j:

@CircuitBreaker(name = "paymentGateway", fallbackMethod = "payFallback")
public PaymentResult processPayment(PaymentRequest req) {
    return externalGateway.charge(req);
}

public PaymentResult payFallback(PaymentRequest req, Exception ex) {
    return PaymentResult.pendingRetry(req.getOrderId());
}

CLOSED → failing 50%+ over 10 calls → OPEN (instant fallback for 30s)
OPEN → probe with 1 real call → if success → CLOSED

The result: payment gateway outage = degraded service, not total failure.
Orders queue for retry. No cascading crash.

#SpringBoot #DistributedSystems #BackendEngineering

---

**POST 15 — Week 8, Tuesday**
Topic: Month 2 Recap

Month 2 complete. Here's the honest scoreboard.

What I built:
✅ Order Management API — live with Swagger UI [link]
✅ LLD designs started — Parking Lot and Vending Machine in lld-java [link]
✅ 160 LeetCode problems solved (93 mediums, 5 hards)
✅ Kafka consumer group with DLQ and Schema Registry running locally
✅ GitHub contribution graph: 60 straight days of green squares

What I struggled with:
→ Dynamic Programming — needed 3 extra days on Week 8 DP problems
→ Kafka transactions — still not fully comfortable with exactly-once

What surprised me:
The biggest learning was TestContainers catching real bugs.
And the N+1 problem — I had NO idea it was this common in production.

Month 3 focus: System Design, advanced K8s, and the flagship project.
The E-Commerce Platform build starts next week.

Day 61 tomorrow.

#LearningInPublic #BackendEngineering #120DayChallenge

---

**POST 16 — Week 8, Friday**
Topic: Poll

Which took you longer to feel confident with?

[ ] System Design
[ ] DSA and LeetCode
[ ] Both took equally long
[ ] Still not confident in either honestly

Caption:
Asking because I'm in Month 2 of a 120-day SDE-2 prep and trying
to understand how to weight the next 60 days. Drop your experience
in the comments — what helped most?

---

**POST 17 — Week 9, Tuesday**
Topic: WhatsApp Architecture Breakdown

How WhatsApp delivers 100 billion messages per day. The architecture
that makes it work.

[ATTACH: Architecture diagram]

Key decisions explained:

1. WebSocket, not HTTP polling
   Persistent connections. Server pushes messages instantly.
   50M concurrent connections across chat servers.

2. Kafka between chat servers
   Sender → your chat server → Kafka → recipient's chat server → recipient
   Decouples the send and receive paths. Survives server restarts.

3. Cassandra for message storage
   Partition key = conversation_id. Cluster key = message_timestamp.
   This makes fetching the last 50 messages of a conversation O(1).

4. Offline delivery
   Message stored in Cassandra. Push notification sent.
   When user comes online: pull pending messages since last seen offset.

5. End-to-end encryption
   Signal protocol. Keys never leave the device. WhatsApp cannot read your messages.

Trade-off I found interesting: read receipts (double blue tick) require
a separate ACK message flow back from recipient to sender through the
same Kafka pipeline. This is why delivery can lag separately from sending.

#SystemDesign #DistributedSystems #BackendEngineering

---

**POST 18 — Week 9, Friday**
Topic: LLD Parking Lot

Parking Lot LLD — here's the full class diagram and every design decision.

GitHub: [link to lld-java/parking-lot]

[ATTACH: Class diagram image]

Design decisions:

1. Singleton for ParkingLot
   There is only one parking lot. getInstance() enforces this.

2. Strategy Pattern for Pricing
   HourlyPricingStrategy, DailyPricingStrategy, WeekendPricingStrategy.
   Adding a new pricing model = new class, zero changes to ParkingLot.

3. Vehicle hierarchy
   Abstract Vehicle → Car, Truck, Motorcycle.
   Each knows its size. ParkingSpot knows its capacity.

4. The concurrency problem (most interviewers ask this):
   Two cars arrive simultaneously for the last available spot.
   Solution: synchronized findAndOccupySpot() method.
   Or for production: SELECT FOR UPDATE on the spot row in PostgreSQL.

5. What I'd do differently at scale:
   Replace in-memory HashMap of spots with Redis.
   This makes it work across multiple parking lot service instances.

#LLD #Java #BackendEngineering

---

**POST 19 — Week 10, Tuesday**
Topic: Kubernetes Zero-Downtime Deployment

How Kubernetes achieves zero-downtime deployments — step by step.

[ATTACH: Rolling update animation or step diagram]

The Rolling Update sequence for 3 pods (maxSurge: 1, maxUnavailable: 0):

Step 1: Kubernetes creates Pod 4 (new version). Now 4 pods running.
Step 2: Kubernetes waits for Pod 4 readinessProbe to return healthy.
Step 3: Kubernetes removes Pod 1 (old version). Back to 3 pods.
Step 4: Repeat for Pod 2, then Pod 3.

The critical detail: Pod 1 is only removed AFTER Pod 4 is READY.
"Ready" = readinessProbe returns 200.

If your readinessProbe checks database connectivity and the new pod
cannot connect: it never becomes Ready, Kubernetes never removes the old pod,
and your deployment rolls back automatically.

Zero-downtime is not magic. It's readiness probes + rolling update config.

One mistake I made: forgetting to add a preStop hook to drain in-flight
requests before the pod is terminated. Without it, users mid-request
get a connection reset.

#Kubernetes #DevOps #BackendEngineering

---

**POST 20 — Week 10, Friday**
Topic: CI/CD Pipeline Walkthrough

My full CI/CD pipeline from push to production. Built with GitHub Actions.

[ATTACH: Pipeline flowchart diagram]

4 stages on every push to main:

1. TEST
   → mvn test (JUnit 5 + TestContainers)
   → jacoco:check (fails if coverage < 80%)
   → Takes: ~45 seconds

2. BUILD
   → Multi-stage Dockerfile (Maven builder + JRE Alpine runtime)
   → docker/build-push-action to GHCR
   → Image tagged with git SHA for traceability
   → Takes: ~90 seconds

3. DEPLOY (Dev)
   → kubectl set image deployment/order-api [new-image-sha]
   → kubectl rollout status (waits for rolling update to complete)
   → Takes: ~30 seconds

4. DEPLOY (Prod)
   → Requires manual approval in GitHub Environment settings
   → Same kubectl commands, different namespace

Total time from push to production: ~3 minutes (+ manual approval).

The GitHub Actions YAML is in the repo: [link]

#DevOps #Kubernetes #BackendEngineering

---

**POST 21 — Week 11, Tuesday**
Topic: AWS Lessons

Deployed to AWS for the first time. Here's what I got wrong.

What I expected: click a few buttons, app runs in the cloud.

What actually happened:

Mistake 1: Opened port 22 to 0.0.0.0/0 in the security group.
Fix: restrict SSH to your IP only. 0.0.0.0/0 is an open door.

Mistake 2: Hard-coded database password in application.yml.
Fix: AWS Secrets Manager → Spring Boot reads at startup via env variable.

Mistake 3: RDS in a public subnet.
Fix: RDS belongs in a private subnet. Only the app server can reach it.

Mistake 4: No CloudWatch alarm on CPU.
Fix: Alert when CPU > 80% for 5 minutes. Caught a runaway query on day 2.

Mistake 5: Deployed with root-level IAM credentials.
Fix: Create an IAM role for the EC2 instance with only the permissions it needs.

The rule: the cloud is secure by default only if you make it insecure.
Every mistake above was a deliberate configuration choice I made wrong.

GitHub: [link to infrastructure notes in docs/]

#AWS #DevOps #BackendEngineering

---

**POST 22 — Week 11, Friday**
Topic: JWT vs OAuth 2.0

JWT and OAuth 2.0 are not the same thing. Here is the difference.

JWT (JSON Web Token):
→ A TOKEN FORMAT. Contains claims (userId, roles, expiry).
→ Self-contained: server validates signature without a database lookup.
→ Stateless: any server with the secret key can validate it.
→ Use when: your own service issues and validates tokens.

OAuth 2.0:
→ An AUTHORIZATION FRAMEWORK. Defines how apps request access to resources.
→ The Authorization Code Flow: user → auth server → code → token exchange.
→ Use when: "Login with Google" or service-to-service permissions.

Where they overlap:
OAuth 2.0 often ISSUES JWTs as access tokens.
They are complementary, not competing.

In my Order Management API:
→ Users authenticate with email/password → server issues a JWT.
→ That JWT is validated by Spring Security on every request.
→ No OAuth needed because there's no third-party identity provider.

If I add "Login with Google": then OAuth 2.0 handles the identity step,
and I still issue my own JWT after validating the Google token.

#SpringBoot #Security #BackendEngineering

---

**POST 23 — Week 12, Tuesday**
Topic: Mock Interview Experience

Did my first full SDE-2 mock interview this week. Here's the honest debrief.

Round: System Design — Design a Notification System

What I did well:
✅ Started with requirements before drawing anything (2 min)
✅ Did capacity estimation before the architecture (1000 notifications/sec → Redis DLQ)
✅ Covered multi-channel (push, email, SMS, in-app)
✅ Explained the Kafka priority queue approach clearly

What I got wrong:
❌ Forgot deduplication entirely (the interviewer had to prompt me)
❌ Did not mention retry logic or dead letter queues until asked
❌ Ran out of time on observability — never covered monitoring

What I'm fixing this week:
- Practising the 5-step framework with strict timers
- Adding deduplication (Redis SET + idempotency key) to every design
- Always covering observability in the last 5 minutes

Scores from my accountability partner: 6.5/10 overall.
Aiming for 8/10 before I sit a real interview.

#SystemDesign #InterviewPrep #BackendEngineering

---

**POST 24 — Week 12, Friday**
Topic: BookMyShow Seat Booking Challenge

The hardest concurrency problem I've solved in production-style code.

Scenario: Coldplay tickets go on sale. 100,000 users try to book seat A5
simultaneously. How do you prevent double-booking?

Bad solution: check availability then book in two steps.
→ Thread 1 reads: A5 is available.
→ Thread 2 reads: A5 is available.
→ Both try to book. One succeeds. One corrupts data.

Solution 1: Redis atomic lock
SET seat:show123:A5 userId EX 600 NX
→ NX means "only set if key does not exist"
→ This is atomic. One thread wins. Others see key already set.
→ EX 600 means the hold expires in 10 minutes if payment doesn't complete.

Solution 2: Database SELECT FOR UPDATE
SELECT * FROM seats WHERE id = 'A5' AND show_id = 123 FOR UPDATE;
→ Row-level lock. One transaction proceeds, others wait.
→ Works, but doesn't scale to 100K concurrent users.

My implementation uses Redis for the hold, PostgreSQL SELECT FOR UPDATE
for the final booking confirmation. Two-phase: fast Redis check, then
durable database commit.

Code: [GitHub link to lld-java/bookmyshow]

#SystemDesign #LLD #Java

---

**POST 25 — Week 13, Tuesday**
Topic: Actively Interviewing Announcement

The 90-day gate check passed. Pipeline is open.

What's live:
→ java-fundamentals: 90 days of Java exercises [link]
→ dsa-java: 240+ LeetCode solutions [link]
→ todo-api: Spring Boot with Flyway, Swagger, TestContainers [link]
→ lld-java: 8 complete designs with UML and tests [link]
→ order-management-api: Kafka, Redis, JWT, circuit breakers [link]
→ scalable-ecommerce-platform: microservices + K8s + CI/CD [link]

What I'm targeting:
Backend SDE-2 roles at Razorpay, PhonePe, Meesho, Atlassian, and Uber.
Java, Spring Boot, Kafka, Kubernetes, distributed systems.

If you're hiring or know someone who is — happy to connect.
Resume available on request.

#OpenToWork #BackendEngineering #SDE2

---

**POST 26 — Week 13, Friday**
Topic: Saga Pattern with Code

When a distributed transaction fails halfway, who cleans up?

Saga pattern — the answer.

Scenario: Order created → Payment charged → Inventory reserved.
Payment succeeds. Inventory reservation fails.

Without Saga: payment is charged, order is stuck. Money taken, item not coming.

With Saga — Choreography approach:

1. OrderService publishes: OrderCreated event
2. PaymentService listens → charges card → publishes: PaymentSucceeded
3. InventoryService listens → tries to reserve → publishes: InventoryFailed
4. PaymentService listens to InventoryFailed → runs compensating transaction: refund card

Each step has a compensating action that undoes it.
No 2-phase commit. No coordinator holding locks.

The code:
@KafkaListener(topics = "inventory.failed")
public void handleInventoryFailed(OrderEvent event) {
    paymentService.refund(event.getPaymentId());
    eventPublisher.publish(new PaymentRefunded(event.getOrderId()));
}

Implemented in scalable-ecommerce-platform: [GitHub link]

#DistributedSystems #Kafka #BackendEngineering

---

**POST 27 — Week 14, Tuesday**
Topic: What Attracts Me to Fintech Engineering

Why I'm specifically targeting fintech backend roles.

The engineering problems are different here.

At a content platform, a bug means users see the wrong post.
At a payment company, a bug means someone's money disappears or appears twice.

That difference changes how you design everything:

→ Idempotency is not optional — every payment endpoint must be safe to retry
→ Exactly-once semantics matter — Kafka transactions, not just at-least-once
→ Reconciliation is a feature — nightly job verifying every transaction matches settlement
→ Availability is measured in nines — 99.99% means 52 minutes of downtime per year

These constraints make fintech backend engineering harder than most,
but also cleaner. The rules are strict. The edge cases are well-defined.

The tech stack at Razorpay and PhonePe is exactly what I've been building:
Java, Spring Boot, Kafka, Redis, PostgreSQL, Kubernetes.

90 days in. The preparation was designed for this.

#Fintech #BackendEngineering #SDE2

---

**POST 28 — Week 14, Friday**
Topic: E-Commerce Platform Launch

The flagship project is complete.

scalable-ecommerce-platform: [GitHub link]

Architecture:
→ 4 microservices: Product, Order, Payment, Notification
→ Spring Cloud Gateway as API Gateway with JWT validation
→ Kafka with Schema Registry (Avro) for all inter-service events
→ Resilience4j circuit breakers on every external call
→ PostgreSQL (separate database per service) + Flyway migrations
→ Redis for distributed caching and rate limiting
→ Kubernetes: HPA, PodDisruptionBudget, NetworkPolicies, RBAC
→ Prometheus + Grafana dashboards for all 4 services
→ GitHub Actions: test → build → push → Helm upgrade on every merge

Load test results (k6, 1000 concurrent users):
→ P99 latency: 287ms
→ Error rate: 0.02%
→ Throughput: 4,200 requests/second

This is the project I'll walk through in every system design interview.
120 days of preparation visible in 6 repositories.

#BackendEngineering #Kubernetes #DistributedSystems

---

**POST 29 — Week 15, Tuesday**
Topic: Interview Experience 1 (Anonymised)

First technical interview done. Here's what happened.

Company: [Tier A product company]
Round: DSA — 1 problem, 45 minutes

Problem: Given a list of meeting intervals, find the minimum number of
conference rooms required. (LeetCode #253)

What I did right:
→ Clarified: are intervals [start, end] inclusive? Are times in minutes?
→ Said the pattern out loud: "This is a heap problem — I'll use a min-heap of end times"
→ Talked through the approach before writing code (interviewer nodded)
→ Finished with 8 minutes to spare
→ Analysed: O(n log n) time, O(n) space — stated before optimisation question

What surprised me:
The interviewer asked "can you optimise the space?" — I hadn't prepared
a two-pointer approach using sorted start/end arrays. Got through it
but took 4 minutes of thinking.

Lesson: for every heap problem, know the two-pointer variant.

Result: Waiting for feedback.

#InterviewPrep #DSA #LeetCode

---

**POST 30 — Week 15, Friday**
Topic: Offer Negotiation Research

Before I negotiate any offer, here's the research I do.

Step 1: Levels.fyi
Search: [Company] [City] SDE-2 [Year]
Note: base, bonus, ESOP, total comp at P25/P50/P75

Step 2: Glassdoor
Filter: Salary, Software Engineer, [City], last 12 months
Cross-reference with Levels.fyi for consistency

Step 3: Blind
Search: "[Company] SDE2 offer" in posts
Most honest data — anonymous, often very recent

Step 4: My floor
After research, I define: minimum base I'll sign for.
Below that, I counter regardless of how excited I am.

Step 5: My anchor
Open negotiations at minimum + 15%.
If they accept immediately, I left money on the table.
If they push back, I have room to concede.

The most important rule:
Never accept verbally. Always in writing.
Always get the written offer letter before resigning.

#CareerStrategy #Negotiation #SDE2

---

**POST 31 — Week 16, Tuesday**
Topic: Offer Received

Offer received. ₹[X] LPA base.

Not sharing the number publicly — but it's where I aimed.
4 months ago the number felt impossible. Today it's in writing.

What actually worked:
→ Public learning — 31 LinkedIn posts drove 3 referrals
→ The projects — interviewers specifically asked about the E-Commerce platform
→ TestContainers — "you test against real PostgreSQL?" got a genuinely impressed reaction in 2 interviews
→ The accountability partner — 8 mock interviews before the real ones started
→ Not skipping Sunday rest — came back sharper every Monday

What I'd remove if I did it again:
→ Week 2 I spent too long on Java theory I could have picked up contextually
→ DP took 2 extra weeks. Should have started it in Week 5, not Week 7.

One more week before I sign. Another round of interviews still open.

#LearningInPublic #BackendEngineering #120DayChallenge

---

**POST 32 — Week 16, Friday**
Topic: Open Source Contribution

Made my first open source contribution this week.

Repository: TheAlgorithms/Java [link to PR]
Contribution: Added Fenwick Tree (Binary Indexed Tree) implementation
with documentation and unit tests.

It took 3 hours. The PR was reviewed and merged in 48 hours.

What this means on a profile:
→ GitHub shows a green square on the contribution graph from a public repo
→ The repository has 57K stars — any visitor to my profile sees the merge
→ The PR itself demonstrates: clean code, unit tests, documentation

What I learned from the review:
The maintainer asked me to add a time complexity comment above each method.
Obvious in retrospect. Added to every future piece of code I write.

For anyone prepping: find a good-first-issue on any popular Java repository.
The contribution matters less than the habit of shipping to external codebases.

#OpenSource #Java #BackendEngineering

---

**POST 33 — Week 17, Tuesday**
Topic: Full Journey Story

120 days. ₹5 LPA → ₹[X] LPA. Here's what it actually took.

The honest version, not the highlight reel:

Day 1: Created two empty GitHub repos. Pushed a README. Called it a win.
Day 8: Could not solve Two Sum without looking it up.
Day 30: First Spring Boot API deployed. Felt like the hardest thing I'd done.
Day 45: Kafka consumer group worked for the first time. Ran around the room.
Day 60: Mock interview: 4.5/10. Partner said "you know the concepts but not the clock."
Day 75: Got a referral from a connection I'd made on LinkedIn 6 weeks earlier.
Day 90: Scalable E-Commerce Platform pushed to GitHub. 6 microservices. Actually works.
Day 103: First real SDE-2 interview. DSA round. Passed.
Day 114: System Design round. Rate Limiter + Notification System combo. Passed.
Day 120: Offer letter arrived. Signed.

What I spent the 120 days on:
→ 300+ LeetCode problems
→ 6 GitHub repositories, 700+ commits
→ 15 system designs, 10 LLD implementations
→ 34 LinkedIn posts, 150+ connections made
→ 8 mock interviews with my accountability partner
→ 200+ applications, 14 interview processes, 3 final rounds, 1 offer

The thing that surprised me most:
The projects did more work than the LeetCode scores.
Interviewers visited GitHub. They saw daily commits.
The E-Commerce platform became a 20-minute conversation in itself.

It is possible. It took 120 days.

#LearningInPublic #BackendEngineering #120DayChallenge

---

**POST 34 — Week 17, Friday**
Topic: Advice for Day 1

What I'd tell myself on Day 1.

1. Start the DSA repository immediately.
   Every problem solved, committed and pushed.
   The contribution graph is a signal. Green from Day 1.

2. Build projects with TestContainers from the start.
   Not H2. Real databases. Real bugs.

3. Write on LinkedIn as you go.
   The audience you build in Week 3 is the referral source in Week 13.

4. Find an accountability partner in Week 1.
   One mock interview per week, every week.
   The 8 mocks were worth more than 100 LeetCode problems.

5. Do not skip Sunday.
   The brain consolidates during rest. Week 1 I worked all 7 days.
   Week 2 I came back slower. Never skipped again after that.

6. The projects matter as much as the DSA.
   You need both. But a live Swagger UI URL in a GitHub README
   is worth 50 LeetCode problems in an interview impression.

7. Apply earlier than you think you should.
   I started applying at Day 60 to practice companies.
   Those interview failures taught me more than Week 5's studying.

Good luck. It is 120 days. It is hard. It is finite.

#BackendEngineering #CareerStrategy #SDE2

---

## Section 6: Automated LinkedIn Engagement System (15 Min/Day)

**The Problem:** Posting 34 times is only half the battle. If you have 200 connections,
your posts will be seen by 40 people. To trigger the LinkedIn algorithm, you need
inbound engagement from engineers at target companies.

**The Solution:** The 15-Minute Target-Engagement Loop.

### Step 1: Set Up The Hit List (Day 1)
Do not use the standard LinkedIn feed. It is algorithmic noise.
1. Search for 50 SDE-2/SDE-3 engineers at Razorpay, PhonePe, Atlassian, Uber, Meesho.
2. Click their profiles → Click the "Bell" icon on their profile (turns on post notifications).
3. Bookmark the URL: `linkedin.com/notifications`

### Step 2: The 15-Minute Daily Loop (Every Day at 15:15)
1. Open the Notifications tab.
2. Look for "X posted for the first time in a while" or "X published a post".
3. **Leave a high-value comment** on 3–5 posts from engineers at your target companies.

### Step 3: What is a "High-Value Comment"?
**Do NOT write:** "Great post!" "CFBR." "Thanks for sharing." (This signals junior).
**DO write (SDE-2 level):**
- *Additive:* "Interesting approach. Have you considered how this handles [Edge Case]?"
- *Validating:* "I ran into this exact issue with Kafka offsets last week. The [Specific Fix] you mentioned is exactly what worked for me."
- *Clarifying:* "Great breakdown. When you mention [Concept], does that mean you bypassed the load balancer entirely?"

### The Algorithm Mechanics:
When you leave a high-value comment on a Senior Engineer's post at Razorpay:
1. They reply to you (LinkedIn algorithm registers a conversation).
2. Their network (other Razorpay engineers) sees your comment.
3. 2–5 people from their network will click your profile.
4. If your profile is optimized (Section 1) and your posts (Section 5) are technical, they will send a connection request.
5. **Result:** You are now building a network of Razorpay engineers organically, without ever sending cold requests. When you eventually apply and ask for a referral, you are already a known entity.

### 6.3 The Profile Completeness Audit (Run on Day 1 and Day 60)

Score your profile against this checklist:

| Element | Present | Score |
|---------|---------|-------|
| Professional photo (face visible, well-lit, neutral background) | | /1 |
| Custom banner (tech logos, personal tagline) | | /1 |
| Headline uses keywords: Java, Spring Boot, Kubernetes, Backend | | /1 |
| About section: hook in first 2 lines before "See more" | | /1 |
| About section: mentions specific project by name | | /1 |
| About section: ends with CTA or status ("Open to SDE-2 roles") | | /1 |
| Experience: all 3 years documented | | /1 |
| Experience: each role has 2–3 impact bullets | | /1 |
| Featured section: GitHub profile link pinned | | /1 |
| Featured section: best project or blog post pinned | | /1 |
| Skills section: Java pinned as #1 | | /1 |
| Skills section: top 5 are technical (not "teamwork") | | /1 |
| Creator Mode enabled | | /1 |
| Open to Work: set to "Recruiters only" | | /1 |
| Education: listed with graduation year | | /1 |

**Score: ___/15. Target ≥ 13/15 before posting any content.**
